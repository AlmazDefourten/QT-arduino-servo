#include <Servo.h>
Servo servo1;
int num;
void setup() {
  servo1.attach(4);

}

void loop() {
  while (Servo.avalable)
  {
    char a = Servo.read();
    static_cast<int>(a);
    if (a == 1)
    {
      servo1.write(num);
      if (num == 360)
      num = 0;
      num = num + 30
    }
  }

}
